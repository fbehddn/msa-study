server:
  port: 8888
spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/study/workspace/native-file-repo
        git:
#          uri: file://Users/youdong-u/study/workspace/git-local-repo
          uri: https://github.com/fbehddn/spring-cloud-config

